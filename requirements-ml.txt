# Optional deep-learning track (ml/). The core hsbg_coach package needs none of this.
# Preferred (declared extra; uv uses the CPU Torch index from pyproject.toml):
#   uv sync --extra full
#   uv run --isolated --extra full pytest -q
# pip equivalent:
#   pip install numpy
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy>=1.24
torch>=2.0
