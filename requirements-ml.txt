# Optional deep-learning track (ml/). The core hsbg_coach package needs none of this.
# CPU is fine for the combat value net; install torch CPU wheels with:
#   pip install numpy
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy>=1.24
torch>=2.0
