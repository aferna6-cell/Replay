# Recorded gameplay trajectories (your data — keep local unless you choose to commit)
/data/*.jsonl
/data/*.jsonl.partial

# Raw Hearthstone client logs may contain account identifiers / BattleTags.
# Keep synthetic regression fixtures under tests/ with a non-.log extension.
Power*.log

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Node (Firestone sim bridge)
bridge/node_modules/
bridge/package-lock.json

# ML model weights / artifacts
*.pt
*.pth

# OS
.DS_Store
*.pt.meta.json
